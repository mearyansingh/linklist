export const metadata = {
   metadataBase: new URL(process.env.URL),
   title: 'Pricing',
}

function PricingPage() {
   return (
      <div className='container-fluid pricing-container'>Pricing Page</div>
   )
}

export default PricingPage